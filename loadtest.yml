config:
  target: "localhost:3000"
phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up
scenarios:
  -name: "get meta info for a product"
  flow:
  - get:
    url: "/reviews/meta/?product_id=4"